package com.example.mystudyhelper.model

class DataRepository {
    // Simula guardar el evento de estrés en una base de datos
    fun saveStressEvent(): Boolean {
        // Aquí en el futuro irá tu código real de base de datos (Room, Firebase, etc.)
        // Por ahora, simulamos que se guardó exitosamente
        return true
    }
}